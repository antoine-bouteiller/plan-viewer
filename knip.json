{
  "$schema": "https://unpkg.com/knip@5/schema.json",
  "entry": ["src/app/frontend.tsx", "extensions/pi/index.ts"],
  "ignore": ["fixtures/**"],
  "ignoreIssues": {
    "src/launcher/registry.ts": ["types"]
  },
  "ignoreDependencies": [
    "@commitlint/cli",
    "bun-plugin-tailwind",
    // keep: lefthook is not installed in CI, so knip would wrongly flag it there
    "lefthook",
    "mermaid"
  ]
}
